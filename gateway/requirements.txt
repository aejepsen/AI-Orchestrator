httpx==0.28.1
fastapi==0.115.0
uvicorn==0.34.0
pydantic==2.11.0
langgraph==0.4.1
langfuse==2.56.1
sentence-transformers==3.4.1
transformers==4.48.0
torch==2.5.1+cpu
# Semiose — Camada A (NER opcional; fallback: regex)
spacy==3.8.4
# Semiose — Camada B (Knowledge Graph; lazy import, degrada sem Neo4j)
neo4j==5.28.1
# Semiose — Camada C Nível 2 (S3): cross-encoder reranker usa sentence-transformers (já incluído acima)
# Semiose — avaliação (S6): BERTScore real é opcional; descomente para habilitar em eval_semiose.py
# bert-score==0.3.13
